#!/usr/bin/env bash
# Configurer localhost pour qu'il résolve vers 127.0.0.2 et facebook.com vers 8.8.8.8

# Configurer de nouvelles résolutions
echo -e "127.0.0.2\tlocalhost" | sudo tee /etc/hosts >/dev/null
echo -e "8.8.8.8\tfacebook.com" | sudo tee -a /etc/hosts >/dev/null
